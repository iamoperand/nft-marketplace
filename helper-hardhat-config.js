const developmentChains = ["hardhat", "localhost"]

const FRONTEND_CONTRACTS_FILE = "../nft-marketplace-ui/constants/network-mapping.json"

module.exports = {
    developmentChains,
    FRONTEND_CONTRACTS_FILE,
}
